# ai/src/prompts/

This directory holds **versioned runtime prompt templates** (markdown / text files with version headers), per `ai/coding_conventions.md` rule 9.
These are distinct from `ai/prompts/phases.md` (which contains coding agent task definitions).
